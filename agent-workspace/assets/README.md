# Assets

## Included

- `data-dreamer-logo-pop-dark.svg` — uploaded DataDreamer logo asset.
- `pi-hud-final-mockups.html` — canonical visual reference.

## Driver

Copy the existing `st7735s.py` driver from `pi-rack-hud` into:

```text
src/pi_hud/drivers/st7735s.py
```

Then apply `agent-workspace/DRIVER_OPTIMIZATION.md`.
